<UserControl x:Class="Presentation.WpfApp.Views.StartView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Presentation.WpfApp.Views"
             
             xmlns:viewmodels="clr-namespace:Presentation.WpfApp.ViewModels"
             xmlns:productVM="clr-namespace:Presentation.WpfApp.ViewModels.ProductViewModels"
             xmlns:categoryVM="clr-namespace:Presentation.WpfApp.ViewModels.CategoryViewModels"
             xmlns:manufacturerVM="clr-namespace:Presentation.WpfApp.ViewModels.ManufacturerViewModels"
             d:DataContext="{d:DesignInstance Type=viewmodels:StartViewModel}"
             
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White" Opacity="0.8">
        <Grid.RowDefinitions>
            <RowDefinition Height="75"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        
            <TextBlock Grid.Row="0" Grid.ColumnSpan="3" Text="Welcome to the Product Management System" 
           HorizontalAlignment="Center" 
           VerticalAlignment="Center"
           Foreground="#e0e0e0"
           FontSize="28"
           FontStyle="Italic" 
           FontWeight="Bold"
           Margin="0 0 0 0">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Foreground" Value="#fff"/>
                        <Setter Property="Effect">
                            <Setter.Value>
                                <DropShadowEffect Color="#000000" BlurRadius="10" ShadowDepth="0"/>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </TextBlock.Style>
            </TextBlock>

        <StackPanel Grid.Row="1" Grid.ColumnSpan="3">
            <TextBlock Text="Filsources are set in: Infrastructure/Configurations/FileSources.cs" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="14" Foreground="#2D4159" FontWeight="Bold" Margin="10 5 10 30"/>
            <TextBlock Text="Use the buttons below to manage products, categories, and manufacturers." HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="14" FontStyle="Italic" Foreground="#2D4159" FontWeight="Bold" Margin="5 20"/>
            <TextBlock Text="Objects in the lists right now" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="16" FontStyle="Italic" Foreground="#2D4159" FontWeight="Bold" Margin="0 30 0 0"/>
        </StackPanel>
        
        <StackPanel Grid.Row="3" Grid.Column="0" Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock FontFamily="Segoe Fluent Icons" FontSize="32" Text="&#xe8f3;" Foreground="SteelBlue" HorizontalAlignment="Center"/>
            <TextBlock Text="{Binding ProductList.Count, StringFormat='Products: {0}'}" FontSize="16" FontStyle="Italic" Foreground="#2D4159" FontWeight="Bold" Margin="5" HorizontalAlignment="Center"/>
            <Button Content="Manage Products" Command="{Binding NavigateToProductListViewCommand}" Width="175" Height="40" HorizontalAlignment="Center"/>
            </StackPanel>

        <StackPanel Grid.Row="3" Grid.Column="1" Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock FontFamily="Segoe Fluent Icons" FontSize="32" Text="&#xe8ec;" Foreground="DarkOrange" HorizontalAlignment="Center"/>
            <TextBlock Text="{Binding CategoryList.Count, StringFormat='Categories: {0}'}" FontSize="16" FontStyle="Italic" Foreground="#2D4159" FontWeight="Bold" Margin="5" HorizontalAlignment="Center"/>
            <Button Content="Manage Categories" Command="{Binding NavigateToCategoryListViewCommand}"  Width="175" Height="40" HorizontalAlignment="Center"/>
            </StackPanel>

        <StackPanel Grid.Row="3" Grid.Column="2" Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock FontFamily="Segoe Fluent Icons" FontSize="32" Text="&#xe731;" Foreground="DarkGreen" HorizontalAlignment="Center"/>
            <TextBlock Text="{Binding ManufacturerList.Count, StringFormat='Manufacturers: {0}'}"  FontSize="16" FontStyle="Italic" Foreground="#2D4159" FontWeight="Bold" Margin="5" HorizontalAlignment="Center"/>
            <Button Content="Manage Manufacturers" Command="{Binding NavigateToManufacturerListViewCommand}"  Width="175" Height="40" HorizontalAlignment="Center"/>
            </StackPanel>
        

    </Grid>
</UserControl>
