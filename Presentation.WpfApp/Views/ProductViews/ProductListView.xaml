<UserControl x:Class="Presentation.WpfApp.Views.ProductViews.ProductListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Presentation.WpfApp.Views.ProductViews"
             
             xmlns:models="clr-namespace:Infrastructure.Models;assembly=Infrastructure"
             
             xmlns:viewmodels="clr-namespace:Presentation.WpfApp.ViewModels"
             xmlns:productVM="clr-namespace:Presentation.WpfApp.ViewModels.ProductViewModels"
             xmlns:categoryVM="clr-namespace:Presentation.WpfApp.ViewModels.CategoryViewModels"
             xmlns:manufacturerVM="clr-namespace:Presentation.WpfApp.ViewModels.ManufacturerViewModels"
             d:DataContext="{d:DesignInstance Type=productVM:ProductListViewModel}"

             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Column="0" Margin="30,10,30,10">
            <Button Command="{Binding NavigateToProductCreateViewCommand}" Content="Create Product" FontStyle="Italic" />
        </StackPanel>

        <ListView ItemsSource="{Binding ProductList}" Grid.Column="1"  Grid.RowSpan="3" Margin="10" HorizontalAlignment="Stretch">
            <ListView.ItemTemplate>
                <DataTemplate DataType="{x:Type models:Product}">
                    <Grid>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <!-- Column 1: Name -->
                            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Left" Width="130">
                                <TextBox Text="Name" BorderThickness="0" Margin="2 0 0 0" FontWeight="Bold" Background="Transparent"/>
                                <TextBlock Text="{Binding ProductName, StringFormat='{} {0}'}" FontWeight="Bold" FontSize="14" Foreground="#2D4159" Background="Transparent"/>
                            </StackPanel>
                            <!-- Column 2: Price -->
                            <StackPanel  VerticalAlignment="Center" HorizontalAlignment="Left" Width="115">
                                <TextBox Text="Price" BorderThickness="0" Margin="2 0 0 0" FontWeight="Bold" Background="Transparent"/>
                                <TextBlock Text="{Binding ProductPrice, StringFormat='{} {0} kr'}" FontWeight="Bold" FontSize="14" Foreground="#2D4159" Background="Transparent" Padding="0,0,10,0"/>
                            </StackPanel>

                            <!-- Column 3: Buttons (span both rows) cleaned -->
                            <StackPanel Orientation="Horizontal" Margin="35 0 0 0">
                                <Button Content="Details" Command="{Binding DataContext.ShowProductDetailsCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Width="60" Height="25" />
                                <Button Content="Edit" Command="{Binding DataContext.NavigateToProductUpdateViewCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Width="60" Height="25" />
                                <Button Content="Delete" Command="{Binding DataContext.DeleteProductCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Width="60" Height="25" />
                            </StackPanel>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <!-- Details panel shown when a listobject is selected -->
        <Border Grid.Row="1" Grid.Column="0" Padding="2" Margin="10 +0"
        BorderBrush="Gray" BorderThickness="1" CornerRadius="2" Background="#FFF">
            <StackPanel>
                <TextBlock Text="{Binding CurrentProductDetails.ProductId}" Margin="0,2" FontSize="10" HorizontalAlignment="Center" TextWrapping="Wrap"/>
                <TextBlock Text="{Binding CurrentProductDetails.ProductName}" FontSize="22" FontWeight="Bold" HorizontalAlignment="Center" TextWrapping="Wrap"/>
                <StackPanel Margin="0 5 0 5" Orientation="Horizontal">
                    <StackPanel HorizontalAlignment="Left" Width="120">
                        <TextBlock Text="{Binding CurrentProductDetails.ProductPrice, StringFormat='  Price: {0} kr'}" Margin="0,2" TextWrapping="Wrap"/>
                   </StackPanel>
                    <StackPanel>
                        <TextBlock Text="{Binding CurrentProductDetails.ProductCode, StringFormat='Art-nr: {0}'}" Margin="0,2" TextWrapping="Wrap"/>
                    </StackPanel>
                </StackPanel>
                <TextBlock Text="Category:" FontWeight="SemiBold" TextWrapping="Wrap"/>
                <TextBlock Text="{Binding CurrentProductDetails.Category.CategoryName}" Margin="15,0,0,0" TextWrapping="Wrap"/>
                <TextBlock Text="Manufacturer:" FontWeight="SemiBold" TextWrapping="Wrap"/>
                <TextBlock Text="{Binding CurrentProductDetails.Manufacturer.ManufacturerName}" Margin="15,0,0,0" TextWrapping="Wrap"/>
                <TextBlock Text="Email:" FontWeight="SemiBold" Margin="0,2,0,0" TextWrapping="Wrap"/>
                <TextBlock Text="{Binding CurrentProductDetails.Manufacturer.ManufacturerEmail}" Margin="15,2,0,0" TextWrapping="Wrap"/>
                <TextBlock Text="Country:" FontWeight="SemiBold" Margin="0,2,0,0" TextWrapping="Wrap"/>
                <TextBlock Text="{Binding CurrentProductDetails.Manufacturer.ManufacturerCountry}" Margin="15,2,0,0" TextWrapping="Wrap"/>
                <TextBlock Text="Description:" FontWeight="SemiBold" Margin="0,2,0,0" TextWrapping="Wrap"/>
                <TextBlock Text="{Binding CurrentProductDetails.ProductDescription}" Margin="15,2,0,0" TextWrapping="Wrap"/>
            </StackPanel>
        </Border>

        <StackPanel Grid.Row="2" Grid.Column="0" Margin="10,10,10,10">
            <Button Content="Back To Startmenu" Command="{Binding NavigateToStartViewCommand}" FontStyle="Italic" Background="#c4c4c4"/>
        </StackPanel>
    </Grid>
</UserControl>
