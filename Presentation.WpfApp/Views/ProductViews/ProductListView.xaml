<UserControl x:Class="Presentation.WpfApp.Views.ProductViews.ProductListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Presentation.WpfApp.Views.ProductViews"
             
             xmlns:viewmodels="clr-namespace:Presentation.WpfApp.ViewModels"
             xmlns:productVM="clr-namespace:Presentation.WpfApp.ViewModels.ProductViewModels"
             xmlns:categoryVM="clr-namespace:Presentation.WpfApp.ViewModels.CategoryViewModels"
             xmlns:manufacturerVM="clr-namespace:Presentation.WpfApp.ViewModels.ManufacturerViewModels"
             d:DataContext="{d:DesignInstance Type=productVM:ProductListViewModel}"
             
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="100"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Column="0" Margin="10">
            <Button FontStyle="Italic" Background="#c4c4c4" Content="Create Product" Margin="0,0,0,10" Command="{Binding NavigateToProductCreateViewCommand}" />
            <Button FontStyle="Italic" Background="#c4c4c4" Content="Update Product" Margin="0,0,0,10"/>
            <Button FontStyle="Italic" Background="#c4c4c4" Content="Delete Product" Margin="0,0,0,10"/>
        </StackPanel>

        <ListView Grid.RowSpan="3" Grid.Column="1" ItemsSource="{Binding Products}" SelectedItem="{Binding SelectedProduct}" Margin="10">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="ID" DisplayMemberBinding="{Binding Id}" Width="100"/>
                    <GridViewColumn Header="Name" DisplayMemberBinding="{Binding Name}" Width="150"/>
                    <GridViewColumn Header="Price" DisplayMemberBinding="{Binding Price}" Width="100"/>
                    <GridViewColumn Header="Category" DisplayMemberBinding="{Binding Category.Name}" Width="100"/>
                    <GridViewColumn Header="Manufacturer" DisplayMemberBinding="{Binding Manufacturer.Name}" Width="100"/>
                </GridView>
            </ListView.View>
        </ListView>

        <!-- Product details panel shown when a product is selected -->
        <Border Grid.Row="1" Grid.Column="0" Padding="10" Margin="10" BorderBrush="Gray" BorderThickness="1" CornerRadius="5" Background="#FFF">
            <StackPanel>
                <TextBlock Text="Produktdetaljer" FontWeight="Bold" FontSize="16" Margin="0,0,0,10"/>
                <TextBlock Text="{Binding SelectedProduct.Name, StringFormat='Namn: {0}'}" Margin="0,2"/>
                <TextBlock Text="{Binding SelectedProduct.Price, StringFormat='Pris: {0:C}'}" Margin="0,2"/>
                <TextBlock Text="{Binding SelectedProduct.Category.Name, StringFormat='Kategori: {0}'}" Margin="0,2"/>
                <TextBlock Text="{Binding SelectedProduct.Manufacturer.Name, StringFormat='Tillverkare: {0}'}" Margin="0,2"/>
            </StackPanel>
        </Border>

        <!-- If no product is selected, show a message -->
        <TextBlock Grid.Row="1" Grid.Column="0" Text="No products available" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        <StackPanel Grid.Row="2" Grid.Column="0" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
            <Button Content="Previous" Command="{Binding NavigateToCategoryCreateViewCommand}" FontStyle="Italic" Background="#c4c4c4" Width="80" Margin="5"/>
            <Button Content="Next" Command="{Binding NavigateToManufacturerCreateViewCommand}" FontStyle="Italic" Background="#c4c4c4" Width="80" Margin="5"/>
        </StackPanel>
    </Grid>
</UserControl>
