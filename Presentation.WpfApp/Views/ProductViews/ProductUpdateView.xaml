<UserControl x:Class="Presentation.WpfApp.Views.ProductViews.ProductUpdateView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Presentation.WpfApp.Views.ProductViews"
             
             xmlns:viewmodels="clr-namespace:Presentation.WpfApp.ViewModels"
             xmlns:productVM="clr-namespace:Presentation.WpfApp.ViewModels.ProductViewModels"
             xmlns:categoryVM="clr-namespace:Presentation.WpfApp.ViewModels.CategoryViewModels"
             xmlns:manufacturerVM="clr-namespace:Presentation.WpfApp.ViewModels.ManufacturerViewModels"
             d:DataContext="{d:DesignInstance Type=productVM:ProductUpdateViewModel}"
             
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <StackPanel VerticalAlignment="Bottom" Grid.Column="0" Margin="10">
            <Button Command="{Binding NavigateToProductListViewCommand}" Content="Back to Product List" FontStyle="Italic" Height="50"  Margin="0,0,0,10"/>
        </StackPanel>
        <StackPanel Grid.Column="1">
            <Label Content="{Binding _currentProductDetails.ProductId}" />
            <TextBox Grid.Row="0" Grid.Column="1" Margin="0,5" Text="{Binding CurrentProductDetails.ProductId}"/>
            <Label Content="{Binding _currentProductDetails.ProductCode}" />
            <TextBox Grid.Row="0" Grid.Column="1" Margin="0,5" Text="{Binding CurrentProductDetails.ProductCode}"/>
            <Label Content="{Binding _currentProductDetails.ProductName}" />
            <TextBox Grid.Row="0" Grid.Column="1" Margin="0,5" Text="{Binding CurrentProductDetails.ProductName}"/>
            <Label Content="{Binding _currentProductDetails.ProductPrice}" />
            <TextBox Grid.Row="0" Grid.Column="1" Margin="0,5" Text="{Binding CurrentProductDetails.ProductPrice}"/>
            <Label Content="{Binding _currentProductDetails.ProductDescription}" />
            <TextBox Grid.Row="0" Grid.Column="1" Margin="0,5" Text="{Binding CurrentProductDetails.ProductDescription}"/>
            <Label Content="{Binding _currentProductDetails.Category.CategoryName}" />
            <TextBox Grid.Row="0" Grid.Column="1" Margin="0,5" Text="{Binding CurrentProductDetails.Category.CategoryName}"/>
            <Label Content="{Binding _currentProductDetails.Manufacturer.ManufacturerName}" />
            <TextBox Grid.Row="0" Grid.Column="1" Margin="0,5" Text="{Binding CurrentProductDetails.Manufacturer.ManufacturerName}"/>
            <Label Content="{Binding _currentProductDetails.Manufacturer.ManufacturerEmail}" />
            <TextBox Grid.Row="0" Grid.Column="1" Margin="0,5" Text="{Binding CurrentProductDetails.Manufacturer.ManufacturerEmail}"/>
            <Label Content="{Binding _currentProductDetails.Manufacturer.ManufacturerCountry}" />
            <TextBox Grid.Row="0" Grid.Column="1" Margin="0,5" Text="{Binding CurrentProductDetails.Manufacturer.ManufacturerCountry}"/>
        </StackPanel>
        <StackPanel VerticalAlignment="Bottom" Grid.Column="1" Margin="10">
            <Button Command="{Binding UpdateProductCommand}" Content="Update Product" FontStyle="Italic" Height="50"  Margin="0,0,0,10"/>
        </StackPanel>
    </Grid>
</UserControl>
