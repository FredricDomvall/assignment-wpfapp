<UserControl x:Class="Presentation.WpfApp.Views.CategoryViews.CategoryCreateView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Presentation.WpfApp.Views.CategoryViews"
             xmlns:categoryVM="clr-namespace:Presentation.WpfApp.ViewModels.CategoryViewModels"
             d:DataContext="{d:DesignInstance Type=categoryVM:CategoryCreateViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Remove Grid.Row/Grid.Column from children inside Border and wrap them in a Grid -->
        <Border BorderThickness="1" BorderBrush="#c9c9c9" CornerRadius="10"  Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Background="#c4c4c4" Opacity="0.9" Padding="0 0 10 10" Margin="100 100 100 0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>

                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Text="{Binding Title, FallbackValue='Create New Category'}" FontWeight="Bold" FontSize="18" FontStyle="Italic" Foreground="WhiteSmoke" Padding="0 10 0 10" HorizontalAlignment="Center"/>
                    
                    <TextBlock Grid.Row="1" Text="Category Name" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="20 5 30 5" FontWeight="DemiBold" FontSize="18" FontStyle="Italic" Foreground="gray"/>
                    <TextBox Grid.Row="1" Grid.Column="2" Text="{Binding NewCategory.CategoryName}" VerticalContentAlignment="Center" Margin="0 0 20 0"/>
                
            </Grid>
        </Border>
        <!-- Submit Button -->
        <Button Grid.Row="2" Grid.Column="0" Content="Cancel" Command="{Binding CancelNewProductCreationCommand}" Width="150" Height="35" HorizontalAlignment="Right" Margin="0 10 75 0"/>
        <!-- Cancel Button -->
        <Button Grid.Row="2" Grid.Column="1" Content="Create Product" Command="{Binding SaveNewProductCommand}" Width="150" Height="35" HorizontalAlignment="Left" Margin="75 10 0 0"/>
    </Grid>
</UserControl>
