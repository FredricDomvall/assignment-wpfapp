<UserControl x:Class="Presentation.WpfApp.Views.CategoryViews.CategoryCreateView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Presentation.WpfApp.Views.CategoryViews"
             xmlns:categoryVM="clr-namespace:Presentation.WpfApp.ViewModels.CategoryViewModels"
             d:DataContext="{d:DesignInstance Type=categoryVM:CategoryCreateViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10" Background="White" Opacity="0.8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="175"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Category Name -->
        <TextBlock Grid.Row="0" Grid.Column="0" Text="Category Name:" VerticalAlignment="Center" Margin="20,5,0,5" FontStyle="Italic" Foreground="Gray"/>
        <TextBox Grid.Row="0" Grid.Column="1" Margin="0,5" Text="{Binding NewCategory.CategoryName, UpdateSourceTrigger=PropertyChanged}"/>

        <!-- Validation Messages -->
        <StackPanel x:Name="ValidationMessagesPanel" Grid.Row="3" Grid.ColumnSpan="2" Orientation="Vertical" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="5,20,5,5">
            <TextBlock Text="* All fields are required." FontStyle="Italic" Foreground="Gray" Height="25" VerticalAlignment="Center" Margin="0,5"/>
        </StackPanel>

        <!-- Submit Button -->
        <Button Grid.Row="3" Grid.Column="1" Content="Create Category" Command="{Binding SaveNewCategoryCommand}" Width="150" Height="35" HorizontalAlignment="Right" Margin="0,10,100,0"/>
        <!-- Cancel Button -->
        <Button Grid.Row="3" Grid.Column="1" Content="Cancel" Command="{Binding CancelNewCategoryCreationCommand}" Width="150" Height="35" HorizontalAlignment="Left" Margin="100,10,25,0"/>
    </Grid>
</UserControl>
